name: APR Day 2 checks

on:
  pull_request:
    paths:
      - '.github/workflows/apr-day2-checks.yml'
      - '.env.example'
      - 'backend/app/apr/**'
      - 'backend/app/core/config.py'
      - 'backend/app/main.py'
      - 'backend/tests/test_apr.py'
      - 'docker-compose.yml'
      - 'docker-compose.dev.yml'
      - 'docs/apr/poc/**'
      - 'frontend/src/app/(apr)/**'
      - 'frontend/tests/app/apr-primeira-conexao.test.tsx'
      - 'frontend/package-lock.json'
      - 'frontend/package.json'

permissions:
  contents: read

jobs:
  apr-backend-tests:
    name: APR backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: pip install -r requirements.txt

      - name: Run APR backend tests
        env:
          DATABASE_URL: 'sqlite+aiosqlite:///:memory:'
          SECRET_KEY: ci-secret-key-for-apr-day2-32b
          RATE_LIMIT_ENABLED: 'false'
        run: pytest tests/test_apr.py -v --no-cov

  apr-frontend-checks:
    name: APR frontend checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '25'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Run frontend lint
        run: npm run lint

      - name: Run frontend type check
        run: npx tsc --noEmit

      - name: Run APR frontend tests
        run: npm run test:run -- apr-primeira-conexao.test.tsx
