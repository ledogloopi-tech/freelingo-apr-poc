/**
 * English curriculum data — re-exported from the language-specific module.
 * All actual data lives in `./en/curriculum.ts`.
 * When a new target language is added, create `./it/curriculum.ts` etc.
 */
export * from './en/curriculum'
