/**
 * English phrasebook data — re-exported from the language-specific module.
 * All actual data lives in `./en/phrasebook.ts`.
 * When a new target language is added, create `./it/phrasebook.ts` etc.
 */
export * from './en/phrasebook'
