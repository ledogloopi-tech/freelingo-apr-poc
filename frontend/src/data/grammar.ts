/**
 * English grammar data — re-exported from the language-specific module.
 * All actual data lives in `./en/grammar.ts`.
 * When a new target language is added, create `./it/grammar.ts` etc.
 */
export * from './en/grammar'
